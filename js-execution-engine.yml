eureka:       
  instance:
    leaseRenewalIntervalInSeconds: 1
    leaseExpirationDurationInSeconds: 2
  client:
    serviceUrl:
      defaultZone: http://localhost:8093/eureka/
    healthcheck:
      enabled: true
    lease:
      duration: 5
management:
  security:
    enabled: false  
logging:
  level:
    com.stckroute.js-execution-engine: DEBUG
